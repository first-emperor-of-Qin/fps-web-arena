# 2026-07-14 工作记录

## 工作室阶段推进决策
- 用户要求"把游戏项目当工作室来推进"，先问阶段。给出 P0–P6 阶梯 + 定位（P0–P3 完成，卡 P4 中段，部署链路断、2MB 单体脆弱、8 文件未提交）。
- 用户选择方向「扩内容/玩法」，并勾选全部 4 支柱：A 多模式PVP / B 武器角色关卡扩量 / C 成长线Meta / D 特殊玩法。
- 关键发现：realtime.js 后端已支持 teamSize（默认3）+ 分队（match_found 下发 team）；前端 WZLB 弹窗被简化为仅 1v1。→ A1 恢复 3v3/5v5 是纯前端、后端就绪的低风险高杠杆项，作为 phase 首个交付。

## 任务清单（见 TaskList，6 个 pillar 任务）
- A1（✅ 已完成）：恢复 3v3/5v5 团队死斗（前端 WZLB 弹窗 + 分队 UI + 房间/匹配 teamSize 选择）。
- A2/A3/B/C/D 待依次推进。

## A1 完成细节（2026-07-14）
- **改动**（仅 index.html，纯前端，后端 realtime.js 早已支持 teamSize）：
  1. WZLB 弹窗（`#wzlb-modal`）新增「选择规模」区：`.wzlb-modes` + 3 个 `.wzlb-mode` 按钮（data-size 1/3/5，1v1 默认 selected）。复用现成 CSS（`.wzlb-mode` 样式已存在）。
  2. 点击接线：弹窗内 `.wzlb-mode` 切 `.selected`（纯 DOM）。
  3. `onlineBtn`(快速匹配) / `roomBtn`(创建房间) 发送时直接从 `#wzlb-modal .wzlb-mode.selected` 读 `data-size` → `teamSize`（不再写死 1，且零 JS 作用域风险）。
  4. `teamQueue`(组队路径) 的规模查询限定到 `#team-panel .wzlb-mode.selected`，防止新弹窗选择器串味。
  5. 文案去掉"仅 1v1"，副标题改"联机团队竞技对决（支持 1v1/3v3/5v5）"。
- **为什么能跑通**：引擎 `startOnlineMatch(mapKey, teamSize, myTeam, remoteList)`(L30829) 本就按 `teamSize*2` 生成、不足用 AI 补齐；服务器 `roomPlayerList` 给每个玩家带 `team`，`match_found`/`room_*` 下发 `teamSize + players{team}`，前端 `startOnlineMatch` 用 `p.team===myTeam` 分队。所以 3v3/5v5 端到端成局。
- **验证**：`curl localhost:3000` 确认运行中服务器已送出 7 处新标记（data-size="3" / _wzlbMsizeBtns / 选择规模 / #wzlb-modal .wzlb-mode.selected），且无遗留 `teamSize:1`。无需重启（静态文件刷新即生效）。
- **测试提示**：3v3 需 6 人在线（最快用「创建房间」+好友邀请/多窗口凑齐 teamSize*2）；匹配队列也按 teamSize 分组。

## 注意
- 改动任何联机相关代码须遵守 HANDOFF 铁律：不在游戏逻辑函数里写联机逻辑，一律走 window.__netHooks / window.MP。
- 改 UI 先 grep 全文件找获胜的 !important 规则。
- 本轮新增/改动文件应在 phase 推进中适时 git 提交（当前 zcode 分支 8 文件未提交）。

## 内容支柱 A2/A3/B/C/D + 跨切任务 完成（2026-07-14 续）
全部按用户"一次性完成、无需询问"指令实现并验证。

### A2 占点/爆破目标模式（✅）
- `setupOnlineObjective()` 改为按 mode 分支：
  - `dom`：3 个占领点（R=36, rate=0.6），任一方同时持有 ≥2 点持续 8s 判胜。
  - `defuse`：单爆破点（0,0, R=30, rate=0.28），占领即引爆获胜；120s 超时按占领进度结算（`_objEnd(0/1)`）。
- `renderObjectiveHud()` 模式化显示（dom/defuse 显示各点占领%，infection 显示感染/人类数 + 倒计时）。

### A3 段位榜（✅）
- 个人面板新增「排行榜」tab + `renderRankList()`；`/api/rank/me` 返回 rank/elo/tier/level/xp/xpNext，面板 `.rd-profile-rank` 同步。

### B 武器/角色/关卡扩量（✅，走 admin 系统）
- `server/seed_config.json` 落库；`/api/config/all` 返回 19 武器 / 10 角色 / 10 关卡，前端 `applyConfig()` 自动浮现，无需改前端硬编码。

### C 成长线 / Meta（✅）
- 后端 `db.js` 新增 `user_progression` 表 + `applyMatchXp`（胜+30/负+12/击杀+3 封顶+60；level=floor(xp/100)+1）。
- `auth.js /api/rank/me` 暴露 level/xp/xpNext。
- 前端个人面板 profile card 加 XP 条 + 等级徽章，`refreshProfileMeta()` 在面板打开时拉取刷新。

### D 特殊玩法 · 生化 infection（✅ 上线）
- WZLB 弹窗新增「生化」按钮（data-mode=infection）。
- `realtime.js` `ALLOWED_MODES` 补 `'infection'`（`normMode` 否则静默回退 tdm → D 冒烟测试曾因此 FAIL，已修）。
- 客户端 `setupInfection()` + `tickInfection()`：房主随机指派感染（<6人1个/≥6人2个），近身 R=3.6 接触感染，人类清零→感染胜、100s 超时→人类胜；`inf_roles`/`inf_infect` WS 事件。

### 跨切任务（✅）
- 性能：spike-aware FPS governor（`worstFrameMs` + `_qualityLockLow` hysteresis）缓解频繁掉帧。
- 背包：从 5 个精简为 3 个（按键 1-3 切换）。
- 个人面板：加简洁大气改版 CSS 覆盖层。

### 验证
- 后端 6 文件 + 前端 5 段 inline script 全部 `node --check` 通过。
- 双客户端冒烟：C 通（A 39XP/Lv1/ELO1016胜、B 15XP/ELO984负），D 通（infection match_found mode=infection）。
- 测试账号已清理，排行榜干净。
- 已提交 zcode 分支。
